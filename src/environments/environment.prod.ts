const packageVars = require('../../package.json');

export const environment = {
  api: 'https://backend.activa.causascomunes.org/',
  imgBase: 'https://backend.activa.causascomunes.org/',
  production: true
};
